import { AppTopBar } from "@/components/layout/app-top-bar";

/** Same shell as the marketing home header; use on all authenticated app surfaces. */
export async function DashboardTopBar() {
  return <AppTopBar />;
}
